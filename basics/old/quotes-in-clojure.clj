; this is the stanard quote macro
(quote (1 2))

; this is shorthand
'(1 2)

; one way to understand the quote macro is "it tells the reader to leave the data structure as-is"
; instead of to treate it like pre-fix function notation
