(ns data-structures.core
  (:gen-class))

(defn -main
  "Defines and uses each data type in Clojure, if the program runs successfully then the sytax is correct."
  [& args]
  (println "Program exit successfully"))
