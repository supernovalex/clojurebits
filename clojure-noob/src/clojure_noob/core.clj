; vim practice too
(ns clojure-noob.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "I'm a little teapot!"))


; find this with grep

  ; these lines were 
  ; all indented
  ; simultaneously with the
  ; > command

; this is a really long line so you want to use the 0 key to reach the first character as fast as possible
